# Simulated AutoDOC demo — Render Static Site
# Publish directory: out
services:
  - type: web
    name: autodoc-simulated-demo
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: out
