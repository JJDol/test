# Simulated AutoDOC demo — Render Web Service (Node)
services:
  - type: web
    name: autodoc-simulated-demo
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
